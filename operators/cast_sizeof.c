#include <stdio.h>


int main ()
{

}

/*
  if int x=0, float y=12.125. x=f -> x= 12(only int portion stored)
  int to float doesn't change the value. (implicit, done for you)
  (explicit is when you do it for yourself(better to do explicit)), called type casting
  (int) 21.55 + (int) 23.43 = 21+23

  sizeof is a special C keyword(like a function).
  * represents a pointer to a variable.
*/
