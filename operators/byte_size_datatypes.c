#include <stdio.h>

int main()
{
  printf("%u, %u, %u, %u, %u %u %u %u\n", sizeof(char), sizeof(short), sizeof(int), sizeof(long), sizeof(long long), sizeof(float), sizeof(double), sizeof(long double));
}

// int i=100;
// float f=331.79;
// double d=8.44e+11;
// char c = 'W';
// _Bool b = 0;
//
// printf("\ni = %d, f = %f, d = %e, d = %g, c = %c, b = %i\n", i, f, d, d, c, b);
